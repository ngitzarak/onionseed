$ sudo apt-get install build-essential libffi-dev python-dev
$ pip3 install blowfish click bcrypt
$ go build


$ python3 onionseed.py --help
Usage: onionseed.py [OPTIONS]

Options:
  --n INTEGER        The nth onionv3 will be generated with the given
                     seedphrase. Default: 1
  --seedphrase TEXT  Specifies seedphrase manually from the command-line and
                     disables prompt.
  --salt TEXT        Choose another salt than $2b$14$8ytOrHOEmDDiPrJArDom9.
  --help             Show this message and exit.
